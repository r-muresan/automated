export const DEFAULT_MAX_ITEMS = 200;
export const ABSOLUTE_MAX_ITEMS = 1000;
export const MAX_PAGES = 50;
/** Stop after this many consecutive pages that yield zero new items. */
export const MAX_CONSECUTIVE_EMPTY = 3;
/** Stop pagination when navigation appears not to change page content repeatedly. */
export const MAX_PAGINATION_STALLS = 2;
